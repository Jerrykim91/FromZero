# 함수 Part2 -> 좀 더 자세하게

# lambda 함수 -> 런타임에 생성해서 사용할수 있는 익명함수
# 일시적인 함수 (함수가 생성된 곳에서만 필요함 )
# 즉, 일반함수와 달리 즉각 그자리에서 쓰고 버리는 함수

# 형태
# lambda 변수1,변수2 :
# lambda 변수1:

a = lambda x, y: x + y 
# => print(a(4, 4))
b = lambda x: x**2  
# => print(b(8))

